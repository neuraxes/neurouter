# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: ""
    version: 0.0.1
paths:
    /v1/chat:
        post:
            tags:
                - Chat
            operationId: Chat_Chat
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/neurouter.v1.ChatReq'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/neurouter.v1.ChatResp'
    /v1/embed:
        post:
            tags:
                - Embedding
            operationId: Embedding_Embed
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/neurouter.v1.EmbedReq'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/neurouter.v1.EmbedResp'
    /v1/models:
        get:
            tags:
                - Model
            operationId: Model_ListModel
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/neurouter.v1.ListModelResp'
components:
    schemas:
        neurouter.v1.ChatReq:
            type: object
            properties:
                id:
                    type: string
                    description: The unique identifier of the chat session
                model:
                    type: string
                    description: The requested model to use
                config:
                    allOf:
                        - $ref: '#/components/schemas/neurouter.v1.GenerationConfig'
                    description: The generation configuration
                messages:
                    type: array
                    items:
                        $ref: '#/components/schemas/neurouter.v1.Message'
                    description: The conversation history
                tools:
                    type: array
                    items:
                        $ref: '#/components/schemas/neurouter.v1.Tool'
                    description: The tools available for the model to use
                metadata:
                    type: object
                    additionalProperties:
                        type: string
                    description: Additional metadata for the request
        neurouter.v1.ChatResp:
            type: object
            properties:
                id:
                    type: string
                    description: The unique identifier of the chat session
                model:
                    type: string
                    description: The model used to generate the response
                status:
                    type: integer
                    description: The status of the response
                    format: enum
                message:
                    allOf:
                        - $ref: '#/components/schemas/neurouter.v1.Message'
                    description: The generated message
                statistics:
                    allOf:
                        - $ref: '#/components/schemas/neurouter.v1.Statistics'
                    description: The statistics for the generation
        neurouter.v1.Content:
            type: object
            properties:
                id:
                    type: string
                    description: The unique identifier of the content
                index:
                    type: integer
                    description: The index of current content
                    format: uint32
                phase:
                    type: integer
                    description: The semantic phase of the content
                    format: enum
                metadata:
                    type: object
                    additionalProperties:
                        type: string
                    description: Additional metadata for the content
                text:
                    type: string
                image:
                    $ref: '#/components/schemas/neurouter.v1.Image'
                toolUse:
                    $ref: '#/components/schemas/neurouter.v1.ToolUse'
                toolResult:
                    $ref: '#/components/schemas/neurouter.v1.ToolResult'
                opaque:
                    type: string
                    description: Opaque content like encrypted CoT
            description: Multi-modality content
        neurouter.v1.EmbedReq:
            type: object
            properties:
                id:
                    type: string
                model:
                    type: string
                contents:
                    type: array
                    items:
                        $ref: '#/components/schemas/neurouter.v1.Content'
        neurouter.v1.EmbedResp:
            type: object
            properties:
                id:
                    type: string
                model:
                    type: string
                embedding:
                    type: array
                    items:
                        type: number
                        format: float
        neurouter.v1.GenerationConfig:
            type: object
            properties:
                maxTokens:
                    type: string
                    description: The maximum number of tokens that can be generated.
                temperature:
                    type: number
                    description: The temperature sampling. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic.
                    format: float
                topP:
                    type: number
                    description: The nucleus sampling, where the model considers the results of the tokens with top_p probability mass.
                    format: float
                topK:
                    type: string
                    description: The top_k sampling, where the model considers the results of the top_k most probable tokens.
                frequencyPenalty:
                    type: number
                    description: Positive values penalize new tokens based on their existing frequency in the text so far, decreasing the model's likelihood to repeat the same line verbatim.
                    format: float
                presencePenalty:
                    type: number
                    description: Positive values penalize new tokens based on whether they appear in the text so far, increasing the model's likelihood to talk about new topics.
                    format: float
                reasoningConfig:
                    allOf:
                        - $ref: '#/components/schemas/neurouter.v1.ReasoningConfig'
                    description: Configuration for reasoning.
                stopSequences:
                    type: array
                    items:
                        type: string
                    description: The sequences where the model will stop generating further tokens.
                presetTemplate:
                    type: string
                presetGrammar:
                    type: string
                    description: Preset grammars, e.g. "text", "json_object", etc.
                gbnfGrammar:
                    type: string
                    description: The GGML BNF grammar definition.
                schema:
                    allOf:
                        - $ref: '#/components/schemas/neurouter.v1.Schema'
                    description: The schema definition.
                jsonSchema:
                    type: string
                    description: The JSON schema definition.
        neurouter.v1.Image:
            type: object
            properties:
                mimeType:
                    type: string
                url:
                    type: string
                data:
                    type: string
                    format: bytes
            description: Represent a image content
        neurouter.v1.ListModelResp:
            type: object
            properties:
                models:
                    type: array
                    items:
                        $ref: '#/components/schemas/neurouter.v1.ModelSpec'
        neurouter.v1.Message:
            type: object
            properties:
                id:
                    type: string
                    description: The unique identifier of the message
                role:
                    type: integer
                    description: The role of the message sender
                    format: enum
                contents:
                    type: array
                    items:
                        $ref: '#/components/schemas/neurouter.v1.Content'
                    description: The multi-modality content
                name:
                    type: string
                    description: The optional name of the message sender
                metadata:
                    type: object
                    additionalProperties:
                        type: string
                    description: Additional metadata for the message
        neurouter.v1.ModelSpec:
            type: object
            properties:
                id:
                    type: string
                    description: The unique identifier of the model.
                name:
                    type: string
                    description: The display name of the model.
                owner:
                    type: string
                    description: The entity that owns the model.
                provider:
                    type: string
                    description: The model service provider.
                modalities:
                    type: array
                    items:
                        type: integer
                        format: enum
                    description: The modalities supported by the model.
                capabilities:
                    type: array
                    items:
                        type: integer
                        format: enum
                    description: The capabilities of the model.
                contextLength:
                    type: integer
                    description: The context length (max tokens) supported by the model.
                    format: uint32
        neurouter.v1.ReasoningConfig:
            type: object
            properties:
                effort:
                    type: integer
                    description: The effort level for reasoning.
                    format: enum
                tokenBudget:
                    type: integer
                    description: The token budget for reasoning.
                    format: uint32
        neurouter.v1.Schema:
            type: object
            properties:
                type:
                    type: integer
                    format: enum
                description:
                    type: string
                minimum:
                    type: number
                    description: The minimum for number
                    format: double
                maximum:
                    type: number
                    description: The maximum for number
                    format: double
                pattern:
                    type: string
                    description: The regex pattern for string
                minLength:
                    type: integer
                    description: The minimum length for string
                    format: int32
                maxLength:
                    type: integer
                    description: The maximum length for string
                    format: int32
                items:
                    allOf:
                        - $ref: '#/components/schemas/neurouter.v1.Schema'
                    description: The schema for array items
                minItems:
                    type: integer
                    description: The minimum number of items for array
                    format: int32
                maxItems:
                    type: integer
                    description: The maximum number of items for array
                    format: int32
                propertyNames:
                    allOf:
                        - $ref: '#/components/schemas/neurouter.v1.Schema'
                    description: The schema for object keys
                properties:
                    type: object
                    additionalProperties:
                        $ref: '#/components/schemas/neurouter.v1.Schema'
                    description: The schema for object values
                required:
                    type: array
                    items:
                        type: string
                    description: The required properties for object
                enum:
                    type: array
                    items:
                        type: string
                allOf:
                    type: array
                    items:
                        $ref: '#/components/schemas/neurouter.v1.Schema'
                anyOf:
                    type: array
                    items:
                        $ref: '#/components/schemas/neurouter.v1.Schema'
                oneOf:
                    type: array
                    items:
                        $ref: '#/components/schemas/neurouter.v1.Schema'
                not:
                    $ref: '#/components/schemas/neurouter.v1.Schema'
        neurouter.v1.Statistics:
            type: object
            properties:
                usage:
                    $ref: '#/components/schemas/neurouter.v1.Statistics_Usage'
            description: Statistics contains token usage information for a model invocation.
        neurouter.v1.Statistics_Usage:
            type: object
            properties:
                inputTokens:
                    type: integer
                    description: Total number of input tokens, including cached tokens.
                    format: uint32
                outputTokens:
                    type: integer
                    description: Number of output tokens generated by the model.
                    format: uint32
                cachedInputTokens:
                    type: integer
                    description: Number of input tokens served from prompt cache (subset of input_tokens).
                    format: uint32
                reasoningTokens:
                    type: integer
                    description: Number of reasoning tokens consumed (subset of output_tokens for reasoning models).
                    format: uint32
        neurouter.v1.Tool:
            type: object
            properties:
                function:
                    $ref: '#/components/schemas/neurouter.v1.Tool_Function'
        neurouter.v1.ToolResult:
            type: object
            properties:
                id:
                    type: string
                outputs:
                    type: array
                    items:
                        $ref: '#/components/schemas/neurouter.v1.ToolResult_Output'
            description: Represent a result from tool
        neurouter.v1.ToolResult_Output:
            type: object
            properties:
                index:
                    type: integer
                    format: uint32
                text:
                    type: string
                image:
                    $ref: '#/components/schemas/neurouter.v1.Image'
        neurouter.v1.ToolUse:
            type: object
            properties:
                id:
                    type: string
                name:
                    type: string
                inputs:
                    type: array
                    items:
                        $ref: '#/components/schemas/neurouter.v1.ToolUse_Input'
            description: Represent a tool use
        neurouter.v1.ToolUse_Input:
            type: object
            properties:
                index:
                    type: integer
                    format: uint32
                text:
                    type: string
        neurouter.v1.Tool_Function:
            type: object
            properties:
                name:
                    type: string
                description:
                    type: string
                inputSchema:
                    $ref: '#/components/schemas/neurouter.v1.Schema'
                inputJsonSchema:
                    type: string
tags:
    - name: Chat
    - name: Embedding
    - name: Model
